@model IEnumerable<thgs.database.Models.User>

@{
    ViewData["Title"] = "Index";
}

@*<h2>Index</h2>

    <p>
        <a asp-action="Create">Create New</a>
    </p>
    <table class="table">
        <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.UserInfo.Name)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.UserInfo.Sex)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.UserInfo.BirthDate)
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.UserInfo.Name)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.UserInfo.Sex)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.UserInfo.BirthDate)
                    </td>
                    <td>
                        <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                        <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                        <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>*@